```Javascript

// Arrow Function  

af→    () => █
afa→   (arg) => █
afad→  ({prop, prop}) => █
afo→   () => ({prop: value█})
afoa→  (arg) => ({prop: value█})
afe→   () => {
  return █
afea→  (arg) => {
  return █
}
afead→ ({prop, prop}) => {
  return █ 
}
afee→  () => {
  █
}
afeea→  (arg) => {
  █
}
afp→  () => {
  (█)
}
afpa→  (arg) => {
  (█)
}

iiaf→  (() => █)()


// Promises

afr→ (response) => █
afrj→ (response) => response.json()█
afrd→ (response) => response.data█
afer→ (response) => {
  return █
}
aferj→ (response) => {
  return response.json()
}█

aferd→ (response) => {
  return response.data
}█

//Arrays

arfeq→ const newArray = array.filter((element) => element === value)█
arfne→ const newArray = array.filter((element) => element !== value)█
arfoeq→ const newArray = array.filter((element) => element.prop === value)█
arfone→ const newArray = array.filter((element) => element.prop !== value)█
arsna→  array.sort((a, b) => a - b)█
arsnd→  array.sort((a, b) => b - a)█
aruv→  const newArray = array.filter((current, index, arr) => arr.indexOf(current) == index)█

// Functions


edaf→ 








```